---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: shankarg-app
  namespace: default
  labels:
    app: shankarg-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shankarg-app
  template:
    metadata:
      labels:
        app: shankarg-app
    spec:
      containers:
      - name: shankarg-app
        image: "dockerawsdevops1/docker-private-repo:latest"
        ports:
          - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: shankarg-app
  namespace: default
spec:
  selector:
    app: shankarg-app
  type: LoadBalancer
  ports:
  - name: http
    targetPort: 3000
    port: 80
