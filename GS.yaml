---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: shankargajula-app
  namespace: default
  labels:
    app: shankargajula-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shankargajula-app
  template:
    metadata:
      labels:
        app: shankargajula-app
    spec:
      containers:
      - name: shankargajula-app
        image: "dockerawsdevops1/docker-private-repo:latest"
        ports:
          - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: shankargajula-app
  namespace: default
spec:
  selector:
    app: shankargajula-app
  type: LoadBalancer
  ports:
  - name: http
    targetPort: 3000
    port: 80
